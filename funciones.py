import math

def triangulo(b,a):
	area = (b * a)/ 2
	return area

def rectangulo(b,a):
        area = (b * a)
        return area
	
def cuadrado(b):
        area = (b * b)
        return area

def circulo(r):
        area = math.pi * (r ** 2)
        return area

